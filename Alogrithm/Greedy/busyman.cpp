#include <bits/stdc++.h>
using namespace std;
#define MAX 100000;
int T, N;
vector<pair<int, int>> v;

int main()
{
  cin >> T; //so test case
  while(T--)
  {
    cin >> N;
    for(int i = 1; i <= N; i++){
      int start, end;
      cin >> start >> end;
      v[sta]
    }
  }


  return 0;
}
